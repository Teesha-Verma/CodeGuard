version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./codeguard.db
      - LLM_PROVIDER=gemini
      - LLM_MODEL=gemini-2.0-flash
    env_file:
      - .env
    volumes:
      - .:/app
